<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link href="/stylesheet/style.css" rel="stylesheet">
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <title>My Saves</title>
  </head>
  <body>
    <!-- Navbar -->
    <%- include('partials/navbar') %>
    <!-- Main -->
    <header class="text-center w-25 mx-auto mt-4 p-2">
      <h2>My Saved Recipes</h2>
    </header>
    <main>
      <!-- My Saves -->
      <div class="container my-4 mx-auto p-4 rounded-3 shadow-md secondary-color">
        <% if (recipesBySaves.length === 0) { %>
          <div class="text-center">You have not saved any recipes.</div>
        <% } %>
        <% let count = recipesBySaves.length - 1 %> <% for (let i=0; i<recipesBySaves.length/2; i++) { %>
        <div class="row mx-auto mb-4">
          <% for (let j=0; j<2; j++) { %> <% if (count !== -1) { %>
          <div class="col-6">
            <div class="card m-auto w-50">
              <img src="<%= recipesBySaves[count].strMealThumb %>" class="card-img-top" alt="<%= recipesBySaves[count].strMeal %>" style="max-width: 410px" />
              <div class="card-body">
                <h5 class="card-title"><%= recipesBySaves[count].strMeal %></h5>
                <a href="/<%= recipesBySaves[count].idMeal %>" class="btn btn-primary">View</a>
              </div>
            </div>
          </div>
          <% } %> <% count-- %> <% } %>
        </div>
        <% } %>
      </div>
    </main>
    <footer>
      <%- include('partials/footer') %>
    </footer>
  </body>
</html>
